// LOKESH: build the real form + validation in this component.

export default function BookingForm() {
  return (
    <form className="card">
      <p className="muted">Lokesh — form fields go here.</p>
    </form>
  );
}
